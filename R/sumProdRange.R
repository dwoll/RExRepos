## ------------------------------------------------------------------------
age <- c(17, 30, 30, 25, 23, 21)
sum(age)
cumsum(age)

## ------------------------------------------------------------------------
diff(age)
diff(age, lag=2)

## ------------------------------------------------------------------------
prod(age)
cumprod(age)
factorial(5)

## ------------------------------------------------------------------------
min(age)
max(age)
range(c(17, 30, 30, 25, 23, 21))
diff(range(c(17, 30, 30, 25, 23, 21)))

## ------------------------------------------------------------------------
which.max(age)

## ------------------------------------------------------------------------
vec <- c(-5, -8, -2, 10, 9)
val <- 0
which.min(abs(vec-val))

## ------------------------------------------------------------------------
vec1 <- c(5, 2, 0, 7)
vec2 <- c(3, 3, 9, 2)
pmax(vec1, vec2)
pmin(vec1, vec2)

